# Leave VITE_API_URL empty for local dev — Vite proxies /api to port 5001
# VITE_API_URL=https://your-api.railway.app

# Show Admin link in header (dev only — do not set in production)
VITE_SHOW_ADMIN_LINK=true
